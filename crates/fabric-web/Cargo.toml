[package]
name = "fabric-web"
version = "0.1.0"
edition = "2021"
description = "Phenotype Fabric web frontend shared types and API contracts"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
fabric-graph = { path = "../fabric-graph" }
fabric-tray = { path = "../fabric-tray" }

serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
